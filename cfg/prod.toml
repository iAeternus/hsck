[smtp_config]
server = "smtp.prod.com"
port = 465
username = "${SMTP_USERNAME}"
password = "${SMTP_PASSWORD}"
encryption = "tls"

[imap_config]
server = "imap.production-server.com"
port = 993
username = "${IMAP_USERNAME}"
password = "${IMAP_PASSWORD}"
out_dir = "/var/data/prod/out"

[stu_config]
list = [
    
]

[log_config]
level = "info"
console_output = false
